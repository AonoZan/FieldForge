name: Test SDF Extension

on:
  workflow_dispatch:
    inputs:
      version_tag:
        description: 'Version tag to fetch and test (e.g., v0.7.2)'
        required: true
        type: string

jobs:
  test-extension:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      